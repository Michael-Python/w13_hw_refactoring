_da6‍.x([["default",()=>_da6‍.o]]);class Library {
  constructor(books = []){
    this.books = books;
  }

  bookCount(){
    return this.books.length;

  }

  addBook(newBook){
    this.books.push(newBook);
  }

  addBooks(newBooks){
    this.books = [...this.books, ...newBooks];
  }

  printInventory() {
    this.books.forEach(({ author, title }) => {
      console.log(`${title} by ${author}`);
    });
  }
}


// var book = newBook;
// module.exports = Library;
_da6‍.d(Library);
